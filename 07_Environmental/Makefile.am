CFLAGS = -Wall 
CONFIG_CLEAN_FILES = config.* *.deps/ *.cache/ compile depcomp *.in install-sh missing rhasher-* *.m4 configure *.txt test-driver

TESTS = test.sh

bin_PROGRAMS = rhasher
rhasher_SOURCES = rhasher.c
